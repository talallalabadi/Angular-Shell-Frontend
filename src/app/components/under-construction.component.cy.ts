import { UnderConstructionComponent } from './under-construction.component'

describe('UnderConstructionComponent', () => {
  it('should mount', () => {
    cy.mount(UnderConstructionComponent)
  })
})